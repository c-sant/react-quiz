import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  slider: {
    color: "#fff",
    width: "90%",
  },
  container: {
    flexDirection: "row",
    width: 200,
  },
  label: {
    color: "#fff",
  },
});

export default styles;
